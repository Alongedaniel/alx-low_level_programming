#include "main.h"

/**
 * print_rev - check the code.
 * @s: Pointer declared.
 * Return: Always 0.
 */

void print_rev(char *s)
{
	int len;
	int i;

	for (len = 0; s[i] != '\0'; len++)
		;
	for (i--; i >= 0; i--)
	{
		_putchar(s[i]);
	}
	_putchar('\n');
}
