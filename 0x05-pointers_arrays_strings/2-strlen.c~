#include "main.h"
#include <string.h>

/**
 * _strlen - check the code
 * @s: pointer variable.
 * Return: Always 0.
 */

int _strlen(char *s)
{
	int len;

	len = strlen(s);
	_puthar(len);
	return (0);
}
